# define N 1000
int lookup[N][N];
void buildSparseTable(int arr[], a,b, int n) 
{ 
    // Initialize M for the intervals with length 1 
    for(int i=1; i<=n; i++)
  
    for(int j=1; (1<<j)<=n; j++)
        continue;
    j--;
    for(int i=1; j
    lookup[a][b]=mi
} 
 
